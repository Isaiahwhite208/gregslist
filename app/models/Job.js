export default class Job {
  constructor(title, profession, hours, wage, degrees, imgUrl) {
    this.title = title
    this.profession = profession
    this.hours = hours
    this.wage = wage
    this.degrees = degrees
    this.imgUrl = imgUrl
  }
}